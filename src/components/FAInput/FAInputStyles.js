import { Input } from "antd";
import styled from "styled-components";

const StyledInput = styled(Input)`
    margin-bottom: 12px;
    border-radius: 8px;
    height: 40px;
`

export {
    StyledInput
}